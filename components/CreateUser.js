import {auth, googleAuthProvider} from '../lib/firebase';

export default function CreateUser({username, password, dateArray}) {
  user = [username, password, date];
  console.log(user.date);
  return user;
}
